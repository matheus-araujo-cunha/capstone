import { Header } from "../../components/Header";
import { UserProfileModal } from "../../components/UserProfileModal";

export const Login = () => {
  return (
    <div>
      <Header />
      <UserProfileModal />
    </div>
  );
};
